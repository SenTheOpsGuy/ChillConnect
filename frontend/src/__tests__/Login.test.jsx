import React from 'react'
import { render } from '@testing-library/react'

// Simple test to ensure test setup is working
describe('Login', () => {
  test('basic test passes', () => {
    expect(2 + 2).toBe(4)
  })
})